---
title: lr login / logout
description: Authenticate the CLI with your LearnRep account.
---

## Login

```bash
lr login
```

Opens a browser OAuth flow. On completion, stores your token at `~/.learnrep/config.json`.

## Logout

```bash
lr logout
```

Removes the stored token.
